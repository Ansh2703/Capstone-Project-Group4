bundle:
  name: "maven_market_v3"

# 1. Loading the resources
include:
  - resources/*.yml

# 2. DEFINING THE VARIABLES (This is what is missing!)
variables:
  target_catalog:
    description: "The catalog where data will be stored"
    default: "maven_market_dev"
  config_file:
    description: "Path to the environment config"
    default: "${workspace.file_path}/config/dev_config.yaml"
  pipelines_development:
    description: "Enable development mode for DLT"
    default: "true"

# 3. Target Overrides
targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405605358658093.13.azuredatabricks.net
      root_path: ~/.bundle/${bundle.name}/dev
    variables:
      target_catalog: "maven_market_dev"
      pipelines_development: "true"

  main:
    mode: production
    workspace:
      host: https://adb-7405605358658093.13.azuredatabricks.net
      root_path: /Shared/.bundle/${bundle.name}/prod
    run_as:
      service_principal_name: "91d7acfb-a1bd-43fb-9024-27fbeb59da96"
    variables:
      target_catalog: "maven_market_uc"
      pipelines_development: "false"